<% if @videos.count > 0 %>
  <ul class="videos-grid small">
    <% @videos.each do |video| %>
      <li>
        <a href="<%= clip_path(video.clip_id) %>">
          <img src="<%= video.cdn_thumbnail_url %>" />
        </a>
      </li>
    <% end %>
  </ul>
<% end %>
